{
  "name": "lambda",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rimraf dist && rimraf lambda.zip",
    "build": "npx tsc",
    "package": "bash package.sh"
  },
  "dependencies": {
    "cowsay": "^1.5.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.101",
    "@types/node": "16",
    "rimraf": "^3.0.2",
    "tsconfig": "*",
    "typescript": "^4.5.3"
  }
}
